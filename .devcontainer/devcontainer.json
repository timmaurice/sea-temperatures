{
  "name": "Home Assistant Dev",
  "image": "ghcr.io/home-assistant/devcontainer:core",
  "postCreateCommand": "python3 -m pip install --upgrade pip && pip3 install -e .[dev] && cd frontend && npm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "keesschollaart.vscode-home-assistant",
        "facebook.pyrefly"
      ]
    }
  }
}
